import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'premium-bonus-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
